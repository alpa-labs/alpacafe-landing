export type ContactContent = {
  subtitle: string;
  title: string;
  intro: string;
  addressLabel: string;
  addressLines: string;
  hoursLabel: string;
  hoursText: string;
  socialLabel: string;
  mapsButtonText: string;
};
