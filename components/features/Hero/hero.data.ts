import type { HeroContent } from './hero.types';
import { siteImages } from '@/lib/constants';

export const heroData: HeroContent = {
  tagline: 'Pasión por el café',
  title: 'Tostamos y servimos café de especialidad',
  ctaText: 'Ver carta completa',
  scrollLabel: 'Bajar',
  backgroundImage: siteImages.hero.background,
};
